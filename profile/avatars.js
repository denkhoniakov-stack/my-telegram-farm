// --- ДАННЫЕ ОБ АВАТАРКАХ ---
// В будущем здесь будут различные аватарки, которые можно разблокировать

const AVATAR_DATA = {
  default: {
    id: 'default',
    name: 'Стандартная аватарка',
    icon: null, // Пустая аватарка
    unlocked: true
  }
  // Здесь можно будет добавить больше аватарок:
  // farmer: { id: 'farmer', name: 'Фермер', icon: '👨‍🌾', unlocked: false },
  // gardener: { id: 'gardener', name: 'Садовник', icon: '👩‍🌾', unlocked: false },
  // и т.д.
};

// Экспортируем данные для использования в других файлах
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { AVATAR_DATA };
}
